apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: oteldemo-gateway
  namespace: otel-demo
spec:
  gatewayClassName: kuma
  listeners:
    - name: http
      port: 80
      protocol: HTTP