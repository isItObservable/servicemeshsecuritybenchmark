# Is it Observable
<p align="center"><img src="/image/logo.png" width="40%" alt="Is It observable Logo" /></p>

## Episode : The serviceMesh BenchMark version 2
This repository contains all the files required to run the test comparing Istio , Linkerd with Kgateway, Kuma, Ambient Mesh, Ambient Mesh with Kgateway
<p align="left "><img src="/image/kuma.png" width="20%" alt="kuma Logo" /><img src="/image/linkerd-icon-color.png" width="20%" alt="linkerd Logo" />
<img src="/image/istio_blue.png" width="20%" alt="istio" /></p>

this repository will use solutions to measure the usage of those agents, we will rely on: 
* the OpenTelemetry Demo


All the observability data generated by the environment would be sent to Dynatrace.


The repo contains a load test scripts agains the opentelemetry demo. the Load test takes 2 hours.
Always leave 2hours after launching the  opentelemetry/loadtest_job.yaml

## Prerequisite
The following tools need to be install on your machine :
- jq
- kubectl
- git
- aws cli ( if you are planning to run this on a EKS)
- eksctl ( if you are planning to run this on a EKS)
- gcloud ( if you are planning to run this on GKE)
- Helm
- step


### 2.Create a eks cluster
First run the following cmd:

```shell
ZONE=eu-west-3
NAME=isitobservable-smbench
OWNER=henrik.rexed
```
* For all the tests that are not related to cilium ( istio, ambientmesh, traefikmesh, kuma, linkerd) : 
```shell
sed -i  '' "s,CLUSTER_NAME_TO_REPLACE,$NAME,"  cluster/cluster.yaml
sed -i  '' "s,REGION_TO_REPLACE,$ZONE,"  cluster/cluster.yaml
sed -i  '' "s,OWNER_TO_REPLACE,$OWNER,"  cluster/cluster.yaml
eksctl create cluster -f cluster/cluster.yaml
```
* For the tests using cilium:
```shell
sed -i  '' "s,CLUSTER_NAME_TO_REPLACE,$NAME,"  cluster/cluster_cilium.yaml
sed -i  '' "s,REGION_TO_REPLACE,$ZONE,"  cluster/cluster_cilium.yaml
sed -i  '' "s,OWNER_TO_REPLACE,$OWNER,"  cluster/cluster_cilium.yaml
eksctl create cluster -f cluster/cluster_cilium.yaml
```

### 3. Clone Github repo

```shell
git clone  https://github.com/isitobservable/servicemeshsecuritybenchmark
cd servicemeshsecuritybenchmark
```
make sure to use the branch v2-gateway-api
## Getting started


### Dynatrace Tenant
#### 1. Dynatrace Tenant - start a trial
If you don't have any Dynatrace tenant , then I suggest to create a trial using the following link : [Dynatrace Trial](https://dt-url.net/observable-trial)
Once you have your Tenant save the Dynatrace tenant url in the variable `DT_TENANT_URL` (for example : https://dedededfrf.live.dynatrace.com)
```
DT_TENANT_URL=<YOUR TENANT Host>
```

##### 2. Create the Dynatrace API Tokens
The dynatrace operator will require to have several tokens:
* Token to deploy and configure the various components
* Token to ingest metrics and Traces


###### Operator Token
One for the operator having the following scope:
* Create ActiveGate tokens
* Read entities
* Read Settings
* Write Settings
* Access problem and event feed, metrics and topology
* Read configuration
* Write configuration
* Paas integration - installer downloader
<p align="center"><img src="/image/operator_token.png" width="40%" alt="operator token" /></p>

Save the value of the token . We will use it later to store in a k8S secret
```shell
API_TOKEN=<YOUR TOKEN VALUE>
```
###### Ingest data token
Create a Dynatrace token with the following scope:
* Ingest metrics (metrics.ingest)
* Ingest logs (logs.ingest)
* Ingest events (events.ingest)
* Ingest OpenTelemetry
* Read metrics
<p align="center"><img src="/image/data_ingest_token.png" width="40%" alt="data token" /></p>
Save the value of the token . We will use it later to store in a k8S secret

```shell
DATA_INGEST_TOKEN=<YOUR TOKEN VALUE>
```

### 1. Deploy the  environment without any ServiceMesh
The application will deploy the entire environment:
```shell

chmod 777 deployment.sh
TYPE=none
./deployment.sh  --clustername "${NAME}" --dturl "${DT_TENANT_URL}" --dtingesttoken "${DATA_INGEST_TOKEN}" --dtoperatortoken "${API_TOKEN}"  --type "${TYPE}"
```

Wait 45min before launching the load test against the applications:
```shell
kubectl apply -f opentelemetry/loadtest_job.yaml -n otel-demo
```


### 2. Deploy the  environment with kuma

The application will deploy the entire environment:
```shell
kubectl delete -f opentelemetry/loadtest_job.yaml -n otel-demo
OLD=$TYPE
TYPE=kuma
chmod 777 update.sh
./update.sh  --type "${TYPE}" --previous "${OLD}"
```

Wait 45min before launching the load test agains the applications:
```shell
kubectl apply -f opentelemetry/loadtest_job.yaml -n otel-demo
```

Deploy Policies
```shell
kubectl delete -f opentelemetry/loadtest_job.yaml -n otel-demo
kubedtl apply -f kuma/policy-kuma.yaml
kubectl apply -f kuma/policy-grpcroute-update.yaml
```
Wait 45min before launching the load test agains the applications:
```shell
kubectl apply -f opentelemetry/loadtest_job.yaml -n otel-demo
```
### 3. Deploy the  environment with Linkerd


The application will deploy the entire environment:
```shell
kubectl delete -f opentelemetry/loadtest_job.yaml -n otel-demo
kubectl delete -f kuma/policy-kuma.yaml
kubectl delete -f kuma/policy-grpcroute-update.yaml
OLD=$TYPE
TYPE=linkerd
chmod 777 update.sh
./update.sh  --type "${TYPE}" --previous "${OLD}"
```

Wait 45min before launching the load test agains the applications:
```shell
kubectl apply -f opentelemetry/loadtest_job.yaml -n otel-demo
```

Deploy Policies
```shell
kubectl delete -f opentelemetry/loadtest_job.yaml -n otel-demo
kubectl apply -f linkerd/policy-linkerd.yaml
kubectl apply -f linkerd/policy-grpcroute-update.yaml
```
Wait 45min before launching the load test agains the applications:
```shell
kubectl apply -f opentelemetry/loadtest_job.yaml -n otel-demo
```


### 5. Deploy the  environment with Istio

The application will deploy the entire environment:
```shell
kubectl delete -f opentelemetry/loadtest_job.yaml -n otel-demo
kubectl delete -f linkerd/policy-grpcroute-update.yaml
kubecrl delete -f linkerd/policy-linkerd.yaml
OLD=$TYPE
TYPE=istio
chmod 777 update.sh
./update.sh  --type "${TYPE}" --previous "${OLD}"
```

Wait 45min before launching the load test against the applications:
```shell
kubectl apply -f opentelemetry/loadtest_job.yaml -n otel-demo
```

Now let's run the same test with Policies
```shell
kubectl delete -f opentelemetry/loadtest_job.yaml -n otel-demo
kubectl apply -f istio/policy-istio.yaml
kubectl apply -f istio/policy-grpcroute-update.yaml
```

Wait 45min before launching the load test agains the applications:
```shell
kubectl apply -f opentelemetry/loadtest_job.yaml -n otel-demo
```


### 5. Deploy the  environment with Ambient

The application will deploy the entire environment:
```shell
kubectl delete -f opentelemetry/loadtest_job.yaml -n otel-demo
kubectl delete -f istio/policy-istio.yaml
kubectl delete -f istio/policy-grpcroute-update.yaml
OLD=$TYPE
TYPE=ambient
chmod 777 update.sh
./update.sh  --type "${TYPE}" --previous "${OLD}"
```

Wait 45min before launching the load test agains the applications:
```shell
kubectl apply -f opentelemetry/loadtest_job.yaml -n otel-demo
```

Now let's run the same test with Policies
```shell
kubectl delete -f opentelemetry/loadtest_job.yaml -n otel-demo
kubectl apply -f istio/ambientmesh/policy-ambient.yaml
kubectl apply -f istio/ambientmesh/policy-grpcroute-update.yaml
```

Wait 45min before launching the load test agains the applications:
```shell
kubectl apply -f opentelemetry/loadtest_job.yaml -n otel-demo
```

### 6. Deploy the  environment with Ambient with the kgateway

The application will deploy the entire environment:
```shell
kubectl delete -f opentelemetry/loadtest_job.yaml -n otel-demo
kubectl delete -f istio/ambientmesh/policy-grpcroute-update.yaml
kubectl delete -f istio/ambientmesh/policy-ambient.yaml
OLD=$TYPE
TYPE=ambient
chmod 777 update.sh
./update.sh  --type "${TYPE}" --previous "${OLD}"
```

Wait 45min before launching the load test agains the applications:
```shell
kubectl apply -f opentelemetry/loadtest_job.yaml -n otel-demo
```



Now let's run the same test with Policies
```shell
kubectl delete -f opentelemetry/loadtest_job.yaml -n otel-demo
kubectl apply -f kgateway-ambient/policy-ambient.yaml
kubectl apply -f kgateway-ambient/policy-kagateway-ambient.yaml
kubectl apply -f kgateway-ambient/policy-grcproute-update.yaml
```

Wait 45min before launching the load test agains the applications:
```shell
kubectl apply -f opentelemetry/loadtest_job.yaml -n otel-demo
```